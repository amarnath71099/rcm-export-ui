// Used for production builds (`ng build` / `ng build --configuration production`).
// Points to the live API hosted on Render.
export const environment = {
  production: true,
  apiUrl: 'https://rcm-export-api.onrender.com/api/entities'
};
